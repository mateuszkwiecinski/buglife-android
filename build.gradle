buildscript {
    repositories {
        google()
        gradlePluginPortal()
    }
    dependencies {
        classpath 'com.project.starter:plugins:0.6.0'
    }
}

ext {
    PUBLISH_VERSION_CODE = 55
    PUBLISH_VERSION_NAME = '1.6.0'
}
